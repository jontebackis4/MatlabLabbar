function [ x ] = fixpt( g, x0 )
%beräknar rot mha fixpunkts metoden
    tol = 0.5e-4;
    x = x0;
    xold = x+1;
    
    while abs(x - xold) > tol
        xtemp
        x = g(xold);
        xold = x;
    end
end

